# Ink notes

- Resource HUD icon system uses a 24×24 grid, 1.5px rounded outline strokes, 2px visual padding, and `currentColor` theming.
- Icon names map directly to resource keys: `wood`, `iron`, `signal`, `reclaimed`.
- HUD SVGs are decorative (`aria-hidden`); the enclosing list item carries the Korean resource label and quantity.
